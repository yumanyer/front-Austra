// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.2
// source: mock.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Empty } from "./google/protobuf/empty.js";

export const protobufPackage = "mock";

export interface CleanUpPreimageShareRequest {
  paymentHash: Uint8Array;
}

export interface UpdateNodesStatusRequest {
  nodeIds: string[];
  status: string;
}

/** TriggerTaskRequest is used to trigger a scheduled task immediately in hermetic tests. */
export interface TriggerTaskRequest {
  taskName: string;
}

export interface QueryPreimageShareRequest {
  paymentHash: Uint8Array;
}

export interface QueryPreimageShareResponse {
  preimageShare: Uint8Array;
  threshold: number;
  invoiceString: string;
}

export interface ModifyNodeTimelockRequest {
  nodeId: string;
  nodeTimelock: number;
  refundTimelock: number;
}

function createBaseCleanUpPreimageShareRequest(): CleanUpPreimageShareRequest {
  return { paymentHash: new Uint8Array(0) };
}

export const CleanUpPreimageShareRequest: MessageFns<CleanUpPreimageShareRequest> = {
  encode(message: CleanUpPreimageShareRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.paymentHash.length !== 0) {
      writer.uint32(10).bytes(message.paymentHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CleanUpPreimageShareRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCleanUpPreimageShareRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.paymentHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CleanUpPreimageShareRequest {
    return { paymentHash: isSet(object.paymentHash) ? bytesFromBase64(object.paymentHash) : new Uint8Array(0) };
  },

  toJSON(message: CleanUpPreimageShareRequest): unknown {
    const obj: any = {};
    if (message.paymentHash.length !== 0) {
      obj.paymentHash = base64FromBytes(message.paymentHash);
    }
    return obj;
  },

  create(base?: DeepPartial<CleanUpPreimageShareRequest>): CleanUpPreimageShareRequest {
    return CleanUpPreimageShareRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CleanUpPreimageShareRequest>): CleanUpPreimageShareRequest {
    const message = createBaseCleanUpPreimageShareRequest();
    message.paymentHash = object.paymentHash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseUpdateNodesStatusRequest(): UpdateNodesStatusRequest {
  return { nodeIds: [], status: "" };
}

export const UpdateNodesStatusRequest: MessageFns<UpdateNodesStatusRequest> = {
  encode(message: UpdateNodesStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.nodeIds) {
      writer.uint32(10).string(v!);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateNodesStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateNodesStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nodeIds.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateNodesStatusRequest {
    return {
      nodeIds: globalThis.Array.isArray(object?.nodeIds) ? object.nodeIds.map((e: any) => globalThis.String(e)) : [],
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: UpdateNodesStatusRequest): unknown {
    const obj: any = {};
    if (message.nodeIds?.length) {
      obj.nodeIds = message.nodeIds;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateNodesStatusRequest>): UpdateNodesStatusRequest {
    return UpdateNodesStatusRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateNodesStatusRequest>): UpdateNodesStatusRequest {
    const message = createBaseUpdateNodesStatusRequest();
    message.nodeIds = object.nodeIds?.map((e) => e) || [];
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseTriggerTaskRequest(): TriggerTaskRequest {
  return { taskName: "" };
}

export const TriggerTaskRequest: MessageFns<TriggerTaskRequest> = {
  encode(message: TriggerTaskRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskName !== "") {
      writer.uint32(10).string(message.taskName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TriggerTaskRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTriggerTaskRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TriggerTaskRequest {
    return { taskName: isSet(object.taskName) ? globalThis.String(object.taskName) : "" };
  },

  toJSON(message: TriggerTaskRequest): unknown {
    const obj: any = {};
    if (message.taskName !== "") {
      obj.taskName = message.taskName;
    }
    return obj;
  },

  create(base?: DeepPartial<TriggerTaskRequest>): TriggerTaskRequest {
    return TriggerTaskRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TriggerTaskRequest>): TriggerTaskRequest {
    const message = createBaseTriggerTaskRequest();
    message.taskName = object.taskName ?? "";
    return message;
  },
};

function createBaseQueryPreimageShareRequest(): QueryPreimageShareRequest {
  return { paymentHash: new Uint8Array(0) };
}

export const QueryPreimageShareRequest: MessageFns<QueryPreimageShareRequest> = {
  encode(message: QueryPreimageShareRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.paymentHash.length !== 0) {
      writer.uint32(10).bytes(message.paymentHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryPreimageShareRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryPreimageShareRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.paymentHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryPreimageShareRequest {
    return { paymentHash: isSet(object.paymentHash) ? bytesFromBase64(object.paymentHash) : new Uint8Array(0) };
  },

  toJSON(message: QueryPreimageShareRequest): unknown {
    const obj: any = {};
    if (message.paymentHash.length !== 0) {
      obj.paymentHash = base64FromBytes(message.paymentHash);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryPreimageShareRequest>): QueryPreimageShareRequest {
    return QueryPreimageShareRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryPreimageShareRequest>): QueryPreimageShareRequest {
    const message = createBaseQueryPreimageShareRequest();
    message.paymentHash = object.paymentHash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseQueryPreimageShareResponse(): QueryPreimageShareResponse {
  return { preimageShare: new Uint8Array(0), threshold: 0, invoiceString: "" };
}

export const QueryPreimageShareResponse: MessageFns<QueryPreimageShareResponse> = {
  encode(message: QueryPreimageShareResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.preimageShare.length !== 0) {
      writer.uint32(10).bytes(message.preimageShare);
    }
    if (message.threshold !== 0) {
      writer.uint32(16).int32(message.threshold);
    }
    if (message.invoiceString !== "") {
      writer.uint32(26).string(message.invoiceString);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryPreimageShareResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryPreimageShareResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.preimageShare = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.threshold = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.invoiceString = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryPreimageShareResponse {
    return {
      preimageShare: isSet(object.preimageShare) ? bytesFromBase64(object.preimageShare) : new Uint8Array(0),
      threshold: isSet(object.threshold) ? globalThis.Number(object.threshold) : 0,
      invoiceString: isSet(object.invoiceString) ? globalThis.String(object.invoiceString) : "",
    };
  },

  toJSON(message: QueryPreimageShareResponse): unknown {
    const obj: any = {};
    if (message.preimageShare.length !== 0) {
      obj.preimageShare = base64FromBytes(message.preimageShare);
    }
    if (message.threshold !== 0) {
      obj.threshold = Math.round(message.threshold);
    }
    if (message.invoiceString !== "") {
      obj.invoiceString = message.invoiceString;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryPreimageShareResponse>): QueryPreimageShareResponse {
    return QueryPreimageShareResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryPreimageShareResponse>): QueryPreimageShareResponse {
    const message = createBaseQueryPreimageShareResponse();
    message.preimageShare = object.preimageShare ?? new Uint8Array(0);
    message.threshold = object.threshold ?? 0;
    message.invoiceString = object.invoiceString ?? "";
    return message;
  },
};

function createBaseModifyNodeTimelockRequest(): ModifyNodeTimelockRequest {
  return { nodeId: "", nodeTimelock: 0, refundTimelock: 0 };
}

export const ModifyNodeTimelockRequest: MessageFns<ModifyNodeTimelockRequest> = {
  encode(message: ModifyNodeTimelockRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nodeId !== "") {
      writer.uint32(10).string(message.nodeId);
    }
    if (message.nodeTimelock !== 0) {
      writer.uint32(16).uint32(message.nodeTimelock);
    }
    if (message.refundTimelock !== 0) {
      writer.uint32(24).uint32(message.refundTimelock);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModifyNodeTimelockRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModifyNodeTimelockRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nodeTimelock = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.refundTimelock = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModifyNodeTimelockRequest {
    return {
      nodeId: isSet(object.nodeId) ? globalThis.String(object.nodeId) : "",
      nodeTimelock: isSet(object.nodeTimelock) ? globalThis.Number(object.nodeTimelock) : 0,
      refundTimelock: isSet(object.refundTimelock) ? globalThis.Number(object.refundTimelock) : 0,
    };
  },

  toJSON(message: ModifyNodeTimelockRequest): unknown {
    const obj: any = {};
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.nodeTimelock !== 0) {
      obj.nodeTimelock = Math.round(message.nodeTimelock);
    }
    if (message.refundTimelock !== 0) {
      obj.refundTimelock = Math.round(message.refundTimelock);
    }
    return obj;
  },

  create(base?: DeepPartial<ModifyNodeTimelockRequest>): ModifyNodeTimelockRequest {
    return ModifyNodeTimelockRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ModifyNodeTimelockRequest>): ModifyNodeTimelockRequest {
    const message = createBaseModifyNodeTimelockRequest();
    message.nodeId = object.nodeId ?? "";
    message.nodeTimelock = object.nodeTimelock ?? 0;
    message.refundTimelock = object.refundTimelock ?? 0;
    return message;
  },
};

export type MockServiceDefinition = typeof MockServiceDefinition;
export const MockServiceDefinition = {
  name: "MockService",
  fullName: "mock.MockService",
  methods: {
    clean_up_preimage_share: {
      name: "clean_up_preimage_share",
      requestType: CleanUpPreimageShareRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {},
    },
    update_nodes_status: {
      name: "update_nodes_status",
      requestType: UpdateNodesStatusRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {},
    },
    /** Triggers the execution of a scheduled task immediately by name. Used by hermetic tests */
    trigger_task: {
      name: "trigger_task",
      requestType: TriggerTaskRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {},
    },
    query_preimage_share: {
      name: "query_preimage_share",
      requestType: QueryPreimageShareRequest,
      requestStream: false,
      responseType: QueryPreimageShareResponse,
      responseStream: false,
      options: {},
    },
    modify_node_timelock: {
      name: "modify_node_timelock",
      requestType: ModifyNodeTimelockRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface MockServiceImplementation<CallContextExt = {}> {
  clean_up_preimage_share(
    request: CleanUpPreimageShareRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<Empty>>;
  update_nodes_status(
    request: UpdateNodesStatusRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<Empty>>;
  /** Triggers the execution of a scheduled task immediately by name. Used by hermetic tests */
  trigger_task(request: TriggerTaskRequest, context: CallContext & CallContextExt): Promise<DeepPartial<Empty>>;
  query_preimage_share(
    request: QueryPreimageShareRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<QueryPreimageShareResponse>>;
  modify_node_timelock(
    request: ModifyNodeTimelockRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<Empty>>;
}

export interface MockServiceClient<CallOptionsExt = {}> {
  clean_up_preimage_share(
    request: DeepPartial<CleanUpPreimageShareRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<Empty>;
  update_nodes_status(
    request: DeepPartial<UpdateNodesStatusRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<Empty>;
  /** Triggers the execution of a scheduled task immediately by name. Used by hermetic tests */
  trigger_task(request: DeepPartial<TriggerTaskRequest>, options?: CallOptions & CallOptionsExt): Promise<Empty>;
  query_preimage_share(
    request: DeepPartial<QueryPreimageShareRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<QueryPreimageShareResponse>;
  modify_node_timelock(
    request: DeepPartial<ModifyNodeTimelockRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<Empty>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
