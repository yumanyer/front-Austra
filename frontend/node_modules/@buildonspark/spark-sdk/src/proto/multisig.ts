// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.2
// source: multisig.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "multisig";

export interface MultisigConfig {
  version: number;
  threshold: number;
  /** Public keys sorted lexicographically. Order determines canonical hash. */
  publicKeys: Uint8Array[];
}

export interface KeyedSignature {
  publicKey: Uint8Array;
  signature: Uint8Array;
}

export interface MultisigSignatureSet {
  multisigConfig: MultisigConfig | undefined;
  signatures: KeyedSignature[];
}

export interface SigningAuthority {
  authority?:
    | { $case: "publicKey"; publicKey: Uint8Array }
    | { $case: "multisig"; multisig: MultisigConfig }
    | undefined;
}

function createBaseMultisigConfig(): MultisigConfig {
  return { version: 0, threshold: 0, publicKeys: [] };
}

export const MultisigConfig: MessageFns<MultisigConfig> = {
  encode(message: MultisigConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.threshold !== 0) {
      writer.uint32(16).uint32(message.threshold);
    }
    for (const v of message.publicKeys) {
      writer.uint32(26).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MultisigConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMultisigConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.threshold = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.publicKeys.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MultisigConfig {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      threshold: isSet(object.threshold) ? globalThis.Number(object.threshold) : 0,
      publicKeys: globalThis.Array.isArray(object?.publicKeys)
        ? object.publicKeys.map((e: any) => bytesFromBase64(e))
        : [],
    };
  },

  toJSON(message: MultisigConfig): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.threshold !== 0) {
      obj.threshold = Math.round(message.threshold);
    }
    if (message.publicKeys?.length) {
      obj.publicKeys = message.publicKeys.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create(base?: DeepPartial<MultisigConfig>): MultisigConfig {
    return MultisigConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MultisigConfig>): MultisigConfig {
    const message = createBaseMultisigConfig();
    message.version = object.version ?? 0;
    message.threshold = object.threshold ?? 0;
    message.publicKeys = object.publicKeys?.map((e) => e) || [];
    return message;
  },
};

function createBaseKeyedSignature(): KeyedSignature {
  return { publicKey: new Uint8Array(0), signature: new Uint8Array(0) };
}

export const KeyedSignature: MessageFns<KeyedSignature> = {
  encode(message: KeyedSignature, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.publicKey.length !== 0) {
      writer.uint32(10).bytes(message.publicKey);
    }
    if (message.signature.length !== 0) {
      writer.uint32(18).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyedSignature {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyedSignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.publicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyedSignature {
    return {
      publicKey: isSet(object.publicKey) ? bytesFromBase64(object.publicKey) : new Uint8Array(0),
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: KeyedSignature): unknown {
    const obj: any = {};
    if (message.publicKey.length !== 0) {
      obj.publicKey = base64FromBytes(message.publicKey);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create(base?: DeepPartial<KeyedSignature>): KeyedSignature {
    return KeyedSignature.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KeyedSignature>): KeyedSignature {
    const message = createBaseKeyedSignature();
    message.publicKey = object.publicKey ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMultisigSignatureSet(): MultisigSignatureSet {
  return { multisigConfig: undefined, signatures: [] };
}

export const MultisigSignatureSet: MessageFns<MultisigSignatureSet> = {
  encode(message: MultisigSignatureSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.multisigConfig !== undefined) {
      MultisigConfig.encode(message.multisigConfig, writer.uint32(10).fork()).join();
    }
    for (const v of message.signatures) {
      KeyedSignature.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MultisigSignatureSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMultisigSignatureSet();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.multisigConfig = MultisigConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.signatures.push(KeyedSignature.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MultisigSignatureSet {
    return {
      multisigConfig: isSet(object.multisigConfig) ? MultisigConfig.fromJSON(object.multisigConfig) : undefined,
      signatures: globalThis.Array.isArray(object?.signatures)
        ? object.signatures.map((e: any) => KeyedSignature.fromJSON(e))
        : [],
    };
  },

  toJSON(message: MultisigSignatureSet): unknown {
    const obj: any = {};
    if (message.multisigConfig !== undefined) {
      obj.multisigConfig = MultisigConfig.toJSON(message.multisigConfig);
    }
    if (message.signatures?.length) {
      obj.signatures = message.signatures.map((e) => KeyedSignature.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<MultisigSignatureSet>): MultisigSignatureSet {
    return MultisigSignatureSet.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MultisigSignatureSet>): MultisigSignatureSet {
    const message = createBaseMultisigSignatureSet();
    message.multisigConfig = (object.multisigConfig !== undefined && object.multisigConfig !== null)
      ? MultisigConfig.fromPartial(object.multisigConfig)
      : undefined;
    message.signatures = object.signatures?.map((e) => KeyedSignature.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSigningAuthority(): SigningAuthority {
  return { authority: undefined };
}

export const SigningAuthority: MessageFns<SigningAuthority> = {
  encode(message: SigningAuthority, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.authority?.$case) {
      case "publicKey":
        writer.uint32(10).bytes(message.authority.publicKey);
        break;
      case "multisig":
        MultisigConfig.encode(message.authority.multisig, writer.uint32(18).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SigningAuthority {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSigningAuthority();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = { $case: "publicKey", publicKey: reader.bytes() };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.authority = { $case: "multisig", multisig: MultisigConfig.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SigningAuthority {
    return {
      authority: isSet(object.publicKey)
        ? { $case: "publicKey", publicKey: bytesFromBase64(object.publicKey) }
        : isSet(object.multisig)
        ? { $case: "multisig", multisig: MultisigConfig.fromJSON(object.multisig) }
        : undefined,
    };
  },

  toJSON(message: SigningAuthority): unknown {
    const obj: any = {};
    if (message.authority?.$case === "publicKey") {
      obj.publicKey = base64FromBytes(message.authority.publicKey);
    } else if (message.authority?.$case === "multisig") {
      obj.multisig = MultisigConfig.toJSON(message.authority.multisig);
    }
    return obj;
  },

  create(base?: DeepPartial<SigningAuthority>): SigningAuthority {
    return SigningAuthority.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SigningAuthority>): SigningAuthority {
    const message = createBaseSigningAuthority();
    switch (object.authority?.$case) {
      case "publicKey": {
        if (object.authority?.publicKey !== undefined && object.authority?.publicKey !== null) {
          message.authority = { $case: "publicKey", publicKey: object.authority.publicKey };
        }
        break;
      }
      case "multisig": {
        if (object.authority?.multisig !== undefined && object.authority?.multisig !== null) {
          message.authority = { $case: "multisig", multisig: MultisigConfig.fromPartial(object.authority.multisig) };
        }
        break;
      }
    }
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
