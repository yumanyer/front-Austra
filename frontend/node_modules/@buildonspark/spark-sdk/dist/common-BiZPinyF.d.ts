import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

//#region src/proto/google/protobuf/empty.d.ts
/**
 * A generic empty message that you can re-use to avoid defining duplicated
 * empty messages in your APIs. A typical example is to use it as the request
 * or the response type of an API method. For instance:
 *
 *     service Foo {
 *       rpc Bar(google.protobuf.Empty) returns (google.protobuf.Empty);
 *     }
 */
interface Empty {}
declare const Empty: MessageFns$1<Empty>;
type Builtin$1 = Date | Function | Uint8Array | string | number | boolean | undefined;
type DeepPartial$1<T> = T extends Builtin$1 ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial$1<U>> : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial$1<U>> : T extends {
  $case: string;
} ? { [K in keyof Omit<T, "$case">]?: DeepPartial$1<T[K]> } & {
  $case: T["$case"];
} : T extends {} ? { [K in keyof T]?: DeepPartial$1<T[K]> } : Partial<T>;
interface MessageFns$1<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial$1<T>): T;
  fromPartial(object: DeepPartial$1<T>): T;
}
declare namespace common_d_exports {
  export { DeepPartial, MessageFns, PackageMap, PackageMap_PackagesEntry, SignatureIntent, SigningCommitment, SigningResult, protobufPackage, signatureIntentFromJSON, signatureIntentToJSON };
}
declare const protobufPackage = "common";
declare enum SignatureIntent {
  CREATION = 0,
  TRANSFER = 1,
  AGGREGATE = 2,
  /** @deprecated */
  REFRESH = 3,
  /** @deprecated */
  EXTEND = 4,
  UNRECOGNIZED = -1
}
declare function signatureIntentFromJSON(object: any): SignatureIntent;
declare function signatureIntentToJSON(object: SignatureIntent): string;
/** A map from a string to a bytes. It's a workaround to have map arrays in proto. */
interface PackageMap {
  packages: {
    [key: string]: Uint8Array;
  };
}
interface PackageMap_PackagesEntry {
  key: string;
  value: Uint8Array;
}
/**
 * A commitment for frost signing.
 * It's a pair of public keys (points) in secp256k1 curve.
 */
interface SigningCommitment {
  /** The public key for hiding. 33 bytes. */
  hiding: Uint8Array;
  /** The public key for binding. 33 bytes. */
  binding: Uint8Array;
}
interface SigningResult {
  signatureShare: Uint8Array;
}
declare const PackageMap: MessageFns<PackageMap>;
declare const PackageMap_PackagesEntry: MessageFns<PackageMap_PackagesEntry>;
declare const SigningCommitment: MessageFns<SigningCommitment>;
declare const SigningResult: MessageFns<SigningResult>;
type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;
type DeepPartial<T> = T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>> : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>> : T extends {
  $case: string;
} ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & {
  $case: T["$case"];
} : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> } : Partial<T>;
interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
//#endregion
export { MessageFns$1 as a, Empty as i, SigningCommitment as n, common_d_exports as r, SignatureIntent as t };