import { AggregateFrostParams, DefaultSparkSigner, SignFrostParams } from "@buildonspark/spark-sdk/bare";
export * from "@buildonspark/spark-sdk/bare";

//#region src/bare-signer.d.ts
/**
 * @deprecated It's no longer necessary to provide BareSparkSigner when
 * initializing a SparkWallet and it will be removed in a future release.
 */
declare class BareSparkSigner extends DefaultSparkSigner {
  signFrost({
    message,
    publicKey,
    verifyingKey,
    selfCommitment,
    statechainCommitments,
    keyDerivation,
    adaptorPubKey
  }: SignFrostParams): Promise<Uint8Array>;
  aggregateFrost({
    message,
    statechainCommitments,
    selfCommitment,
    statechainSignatures,
    selfSignature,
    statechainPublicKeys,
    publicKey,
    verifyingKey,
    adaptorPubKey
  }: AggregateFrostParams): Promise<Uint8Array>;
}
//#endregion
export { BareSparkSigner };