Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
//#region \0rolldown/runtime.js
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
	if (from && typeof from === "object" || typeof from === "function") {
		for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
			key = keys[i];
			if (!__hasOwnProp.call(to, key) && key !== except) {
				__defProp(to, key, {
					get: ((k) => from[k]).bind(null, key),
					enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
				});
			}
		}
	}
	return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", {
	value: mod,
	enumerable: true
}) : target, mod));

//#endregion
require("bare-node-runtime/global");
let module$1 = require("module");
module$1 = __toESM(module$1);
let _buildonspark_spark_frost_bare_addon = require("@buildonspark/spark-frost-bare-addon");
let _buildonspark_spark_sdk_bare = require("@buildonspark/spark-sdk/bare");

//#region src/bare-signer.ts
/**
* @deprecated It's no longer necessary to provide BareSparkSigner when
* initializing a SparkWallet and it will be removed in a future release.
*/
var BareSparkSigner = class extends _buildonspark_spark_sdk_bare.DefaultSparkSigner {
	async signFrost({ message, publicKey, verifyingKey, selfCommitment, statechainCommitments, keyDerivation, adaptorPubKey }) {
		const signingPrivateKey = await this.getSigningPrivateKeyFromDerivation(keyDerivation);
		if (!signingPrivateKey) throw new _buildonspark_spark_sdk_bare.SparkValidationError("Private key not found for public key", { field: "privateKey" });
		const commitment = selfCommitment.commitment;
		const nonce = this.commitmentToNonceMap.get(commitment);
		if (!nonce) throw new _buildonspark_spark_sdk_bare.SparkValidationError("Nonce not found for commitment", { field: "nonce" });
		const keyPackage = {
			secretKey: signingPrivateKey,
			publicKey,
			verifyingKey
		};
		return (0, _buildonspark_spark_sdk_bare.getSparkFrost)().signFrost({
			message,
			keyPackage,
			nonce,
			selfCommitment: selfCommitment.commitment,
			statechainCommitments,
			adaptorPubKey
		});
	}
	async aggregateFrost({ message, statechainCommitments, selfCommitment, statechainSignatures, selfSignature, statechainPublicKeys, publicKey, verifyingKey, adaptorPubKey }) {
		return (0, _buildonspark_spark_sdk_bare.getSparkFrost)().aggregateFrost({
			message,
			statechainCommitments,
			selfCommitment: selfCommitment.commitment,
			statechainSignatures,
			selfSignature,
			statechainPublicKeys,
			selfPublicKey: publicKey,
			verifyingKey,
			adaptorPubKey
		});
	}
};

//#endregion
//#region src/entry.ts
Object.defineProperty(module$1.default, "_resolveFilename", {
	value: () => {
		throw new Error("@buildonspark/bare: This method is not supported in bare.");
	},
	writable: false,
	enumerable: false,
	configurable: false
});
var SparkFrostBare = class extends _buildonspark_spark_sdk_bare.SparkFrostBase {
	signFrost({ message, keyPackage, nonce, selfCommitment, statechainCommitments, adaptorPubKey }) {
		return (0, _buildonspark_spark_frost_bare_addon.signFrost)(message, keyPackage, nonce, selfCommitment, statechainCommitments ? Object.entries(statechainCommitments) : [], adaptorPubKey || new Uint8Array(0));
	}
	aggregateFrost({ message, statechainCommitments, selfCommitment, statechainSignatures, selfSignature, statechainPublicKeys, selfPublicKey, verifyingKey, adaptorPubKey }) {
		return (0, _buildonspark_spark_frost_bare_addon.aggregateFrost)(message, statechainCommitments ? Object.entries(statechainCommitments) : [], selfCommitment, statechainSignatures ? Object.entries(statechainSignatures) : [], selfSignature, statechainPublicKeys ? Object.entries(statechainPublicKeys) : [], selfPublicKey, verifyingKey, adaptorPubKey || new Uint8Array(0));
	}
	createDummyTx(address, amountSats) {
		return (0, _buildonspark_spark_frost_bare_addon.createDummyTx)(address, amountSats);
	}
	encryptEcies(msg, publicKey) {
		return (0, _buildonspark_spark_frost_bare_addon.encryptEcies)(msg, publicKey);
	}
	decryptEcies(encryptedMsg, privateKey) {
		return (0, _buildonspark_spark_frost_bare_addon.decryptEcies)(encryptedMsg, privateKey);
	}
	splitSecretWithProofs(secret, threshold, numShares) {
		const result = (0, _buildonspark_spark_frost_bare_addon.splitSecretWithProofs)(secret, threshold, numShares);
		return Promise.resolve(result.map((s) => ({
			threshold: s.threshold,
			index: s.index,
			share: new Uint8Array(s.share),
			proofs: s.proofs.map((p) => new Uint8Array(p))
		})));
	}
	recoverSecret(shares) {
		const result = (0, _buildonspark_spark_frost_bare_addon.recoverSecret)(shares);
		return Promise.resolve(result);
	}
	validateShare(share, index, threshold, proofs) {
		(0, _buildonspark_spark_frost_bare_addon.validateShare)(share, index, threshold, proofs);
		return Promise.resolve();
	}
	constructNodeTxPair(parentTx, vout, address, sequence, directSequence, feeSats) {
		return (0, _buildonspark_spark_frost_bare_addon.constructNodeTxPair)(new Uint8Array(parentTx), vout, address, sequence, directSequence, feeSats);
	}
	constructRefundTxTrio(cpfpNodeTx, directNodeTx, vout, receivingPubkey, network, sequence, directSequence, feeSats) {
		return (0, _buildonspark_spark_frost_bare_addon.constructRefundTxTrio)(new Uint8Array(cpfpNodeTx), directNodeTx ? new Uint8Array(directNodeTx) : new Uint8Array(0), vout, new Uint8Array(receivingPubkey), network, sequence, directSequence, feeSats);
	}
	computeMultiInputSighash(tx, inputIndex, prevOutScripts, prevOutValues) {
		return (0, _buildonspark_spark_frost_bare_addon.computeMultiInputSighash)(new Uint8Array(tx), inputIndex, prevOutScripts.map((s) => new Uint8Array(s)), prevOutValues);
	}
};
(0, _buildonspark_spark_sdk_bare.setSparkFrostOnce)(new SparkFrostBare());

//#endregion
exports.BareSparkSigner = BareSparkSigner;
Object.keys(_buildonspark_spark_sdk_bare).forEach(function (k) {
  if (k !== 'default' && !Object.prototype.hasOwnProperty.call(exports, k)) Object.defineProperty(exports, k, {
    enumerable: true,
    get: function () { return _buildonspark_spark_sdk_bare[k]; }
  });
});
