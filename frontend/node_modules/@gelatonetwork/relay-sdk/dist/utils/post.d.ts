import { Config, RelayCall, SafeRequestPayload } from "../lib/types";
export declare const post: <Request, Response>(payload: {
    relayCall: RelayCall;
    request: SafeRequestPayload<Request>;
}, config: Config) => Promise<Response>;
